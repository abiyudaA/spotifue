<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Profile - Spotifue</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css"
    />
  </head>
  <body class="bg-gray-900 text-white h-screen flex items-center justify-center">
    <div class="bg-gray-800 p-12 rounded-lg shadow-lg w-2/5 text-center">
      <img
        src="<%= getProfile.profilePicture %>"
        alt="Profile Picture"
        class="w-32 h-32 rounded-full mx-auto mb-6"
      />
      <% if (deleted) { %>
        <div class="bg-green-500/20 p-4 rounded-lg mb-4 border border-green-500">
          <h2 class="text-green-500 font-semibold">Lagu dengan judul <%= deleted %> berhasil dihapus!</h2>
        </div>
      <% } %>
      <h2 class="text-3xl font-bold"><%= getProfile.profileName %></h2>
      <p class="text-gray-400 mb-6 text-lg"><%= getProfile.bio %></p>
      <% songs.forEach(s => { %>
        <li class="p-2 bg-gray-700 rounded-lg hover:bg-gray-600 mb-4 flex justify-between items-center transition-all duration-200 group">
          <div class="flex items-center space-x-4 flex-1">
            <img
              src="/uploads/<%= s.songImage %>"
              alt=""
              class="w-16 h-16 rounded-lg"
            />
            <div class="text-left">
              <h3 class="font-bold text-lg">🎵 <%= s.title %></h3>
              <p class="text-gray-300 text-sm">⏳ <%= s.minutes %> menit</p>
            </div>
          </div>
          <a 
            href="/home/delete/<%= s.id %>" 
            class="hover:scale-105 transition-transform"
          >
            <button class="bg-red-600/80 hover:bg-red-700 text-white px-4 py-2 rounded-lg font-semibold text-sm flex items-center gap-2 shadow-lg hover:shadow-red-500/30">
              🗑 Delete
            </button>
          </a>
          <a href="/home/edit/<%= s.id %>" id="">
            <button>Edit</button>
          </a>
        </li>
      <% }) %>
      
      <div class="mt-8 flex justify-center gap-4">
        <a
          href="/profile/edit"
          class="bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded-full font-semibold text-sm transition-all duration-200 transform hover:scale-105 shadow-lg hover:shadow-blue-500/30 flex items-center gap-2"
        >
          ✏️ Edit Profile
        </a>
        
        <a
          href="/home/add"
          class="bg-emerald-600 hover:bg-emerald-700 text-white px-6 py-3 rounded-full font-semibold text-sm transition-all duration-200 transform hover:scale-105 shadow-lg hover:shadow-emerald-500/30 flex items-center gap-2"
        >
          ➕ Tambah Lagu
        </a>
      </div>

      <div class="mt-6">
        <a
          href="/home"
          class="inline-block bg-gray-700 hover:bg-gray-600 text-white px-6 py-2 rounded-full text-sm font-medium transition-colors duration-200"
        >
          🏠 Kembali ke Home
        </a>
      </div>
    </div>
  </body>
</html>