<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css">
</head>

<body class="bg-gray-900 flex items-center justify-center min-h-screen">
    <div class="bg-gray-800 p-8 rounded-lg shadow-lg w-96">
        <h2 class="text-white text-2xl font-bold text-center mb-4">Join the Spotifue Now!</h2>
        <% if (errors) { %>
            <% errors.forEach(err => { %>
             <h3 style="color: aliceblue;"><%= err %></h3>
            <% }) %>
        <% } %>
        <form action="/register" method="POST" class="space-y-4">
            <div>
                <label for="email" class="text-gray-300 block mb-1">Email</label>
                <input type="email" id="email" name="email" 
                    class="w-full p-2 rounded bg-gray-700 text-white focus:ring focus:ring-indigo-500">
            </div>
            <div>
                <label for="username" class="text-gray-300 block mb-1">Username</label>
                <input type="text" id="username" name="username" 
                    class="w-full p-2 rounded bg-gray-700 text-white focus:ring focus:ring-indigo-500">
            </div>
            <div>
                <label for="password" class="text-gray-300 block mb-1">Password</label>
                <input type="password" id="password" name="password" 
                    class="w-full p-2 rounded bg-gray-700 text-white focus:ring focus:ring-indigo-500">
            </div>
            <div>
                <label for="role" class="text-gray-300 block mb-1">Register as</label>
                <select id="role" name="role"
                    class="w-full p-2 rounded bg-gray-700 text-white focus:ring focus:ring-indigo-500">
                    <option value="artist">🎤 Artist</option>
                    <option value="listener">🎧 Listener</option>
                </select>
            </div>
            <button type="submit" 
                class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-2 px-4 rounded">Register</button>
        </form>
        <p class="text-gray-400 text-center mt-4">Already have an account? <a href="/login" class="text-indigo-400">Log in</a></p>
    </div>

</body>
</html>