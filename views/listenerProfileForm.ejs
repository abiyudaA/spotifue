<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css">
</head>
<body class="bg-gray-900 text-white p-6">

    <h2 class="text-2xl font-bold mb-4">Edit Profile</h2>

    <form action="/submit-profile" method="POST" enctype="multipart/form-data">
        <!-- Profile Name -->
        <div class="mb-4">
            <label for="profileName" class="block text-sm font-medium">Profile Name</label>
            <input type="text" id="profileName" name="profileName" required class="w-full p-2 mt-2 rounded bg-gray-700 text-white" placeholder="Enter your profile name">
        </div>

        <!-- Biography -->
        <div class="mb-4">
            <label for="biography" class="block text-sm font-medium">Biography</label>
            <textarea id="biography" name="biography" required class="w-full p-2 mt-2 rounded bg-gray-700 text-white" rows="4" placeholder="Tell us something about yourself"></textarea>
        </div>

        <!-- Profile Picture -->
        <div class="mb-4">
            <label for="profilePicture" class="block text-sm font-medium">Profile Picture</label>
            <input type="file" id="profilePicture" name="profilePicture" accept="image/*" required class="w-full p-2 mt-2 rounded bg-gray-700 text-white">
        </div>

        <!-- Submit Button -->
        <div>
            <button type="submit" class="bg-blue-600 hover:bg-blue-500 text-white py-2 px-4 rounded">Update Profile</button>
        </div>
    </form>

</body>
</html>
